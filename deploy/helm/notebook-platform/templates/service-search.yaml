{{- if .Values.services.search.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "notebook-platform.componentName" (dict "root" . "component" "search") }}
  namespace: {{ include "notebook-platform.namespace" . }}
  labels:
    {{- include "notebook-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: search
spec:
  type: ClusterIP
  selector:
    {{- include "notebook-platform.selectorLabels" (dict "root" . "component" "search") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.services.search.port }}
      targetPort: http
{{- end }}

{{- if .Values.services.notification.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "notebook-platform.componentName" (dict "root" . "component" "notification") }}
  namespace: {{ include "notebook-platform.namespace" . }}
  labels:
    {{- include "notebook-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: notification
spec:
  type: ClusterIP
  selector:
    {{- include "notebook-platform.selectorLabels" (dict "root" . "component" "notification") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.services.notification.port }}
      targetPort: http
{{- end }}

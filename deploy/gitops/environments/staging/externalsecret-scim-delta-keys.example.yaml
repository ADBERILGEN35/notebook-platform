# Faz 118 — ExternalSecret fragment (EXAMPLE ONLY).
#
# Append to the staging ExternalSecret that targets notebook-platform-secrets.
# Remote paths are environment-specific placeholders — replace with your secret manager paths.
#
#   - secretKey: scim-delta-remote-bearer-token
#     remoteRef:
#       key: <secret-manager-path>/scim-delta-sandbox-bearer
#       property: token

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: notebook-platform-dev
  namespace: argocd
spec:
  project: notebook-platform
  sources:
    - repoURL: https://github.com/ORG/REPO.git
      targetRevision: main
      path: deploy/helm/notebook-platform
      helm:
        valueFiles:
          - $values/deploy/gitops/environments/dev/values.yaml
    - repoURL: https://github.com/ORG/REPO.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: notebook-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
